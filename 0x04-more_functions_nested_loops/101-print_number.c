#include "main.h"
/**
 * print_number - prints integers
 * @n: Accepts integer
 *
 * Return: Nothing (always)
 */
void print_number(int n)
{
	int number = n;

	if (number < 0)
	{
		_putchar('-');
	}
}
